
@{
    ViewBag.Title = "Nóminas procesadas";
    if (Session["IdUsuario"] == null || Session["IdEmpresa"] == null)
    {
        Response.Redirect("~/Account/Login");
    }
}
@using (Html.BeginForm())
{
    @Html.Action("GridViewPartial_ro_rol")
}
