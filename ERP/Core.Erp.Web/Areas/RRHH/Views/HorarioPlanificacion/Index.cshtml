
@{
    ViewBag.Title = "Planificaciones";
    if (Session["IdUsuario"] == null || Session["IdEmpresa"] == null)
    {
        Response.Redirect("~/Account/Login");
    }
}

@using (Html.BeginForm())
{
    @Html.Action("GridViewPartial_horario_planificacion")
}
