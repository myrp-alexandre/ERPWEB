CREATE TABLE [dbo].[tb_parametro_tipo] (
    [IdTipoParam]   VARCHAR (50) NOT NULL,
    [nom_TipoParam] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_tb_parametro_tipo] PRIMARY KEY CLUSTERED ([IdTipoParam] ASC)
);

